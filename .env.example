# Скопируйте в .env (файл .env в репозиторий не коммитится)
DATABASE_URL="file:./dev.db"
# Минимум 32 символа. Сгенерировать: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
SESSION_SECRET=""